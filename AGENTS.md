# Peak Studios agent guidance

Use the `peak-scripts` skill and the canonical catalog in `catalog/catalog.json` for Peak Studios script questions.

- Ground claims in an indexed public source URL and source commit.
- Inspect the user's installed resource before changing it.
- Mark uncited compatibility or behavior as inference.
- Never imply access to private Peak Studios source.
- Keep the MCP service read-only; do not add mutation tools.
